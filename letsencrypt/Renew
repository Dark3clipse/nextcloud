FROM clover/certbot:latest
MAINTAINER shadash <sophiahadash@gmail.com>

RUN echo "0 0 1 * * root certbot renew --webroot -w /data/letsencrypt --quiet" >/etc/cron.d/certbot

ENTRYPOINT []
CMD ["crond", "-f", "-d", "1"]